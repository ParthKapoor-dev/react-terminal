

const output = "The `cd` and `ls` commands are basic commands used in Unix-like operating systems, such as Linux. `cd` (change directory): This command is used to change the current working directory. For example, `cd /home/parth` would change the directory to `/home/parth`. - `ls` (list): This c ";

export const dummyChatResponse : string = output; 